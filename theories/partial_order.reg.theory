/* Theory in regular logic that axiomatizes the concept of a partial order */
{
  subtheories {
    implicit {
      preorder: @preorder
    }
  },
  axioms {
    assertions {
      [x : D] [y : D] (lessThanOrEq(x, y) ∧ lessThanOrEq(y, x)) → (x = y); // Symmetry
    }
  }
}